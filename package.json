{
  "private": true,
  "name": "bad-weather-sms-alert",
  "version": "1.0.0",
  "description": "A demo of using cron triggers for Cloudflare Workers to check for bad weather and send a SMS using Twilio to let us know.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Scoticus <scoticus@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
